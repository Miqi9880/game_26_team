# ROS 2 and colcon build products
/build/
/install/
/log/

# Local offline-test outputs written at the repository root
/*.csv
/*.png
/*.jpg
/*.jpeg

# Local model/cache directories (real model assets must be reviewed explicitly)
/.model_cache/
/.cache/
__pycache__/
